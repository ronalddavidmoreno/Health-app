'use strict'
function enviarForm() {
    // location.href='interfaces/dashboard/paciente/perfil-paciente.html';
}
// var eventEnviar = document.getElementById('enviar');
// eventEnviar.addEventListener("onclick", enviarForm(), false);

function login() {
    var elemento = document.querySelector('.login')
    elemento.style.display = 'block';
}